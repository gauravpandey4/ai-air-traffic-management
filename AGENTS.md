# FutureATC Lab Agent Instructions

Before acting in this repository:

1. Read `.codex/MEMORY.md`.
2. Read `.codex/task-memory.md`.
3. Check `git status --short --branch` and the current branch.
4. Refresh relevant GitHub pull-request, Actions, and Pages state.
5. Reconcile live state with task memory and continue from the recorded exact next action.

Follow the approved five-gate workflow. Do not deploy before explicit Gate 4 approval. Never commit the private source report, enrollment information, credentials, private contact details, local machine paths, or an unapproved student name.

Use deterministic, explainable educational logic and preserve the visible academic safety disclaimer. External data must remain optional, validated, attributed, and unable to break Simulation mode.

After every command failure, decision, commit, push, PR result, merge, or deployment event, update both memory files with concise evidence before handing work to another agent.
